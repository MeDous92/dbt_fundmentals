version: 2

sources:
  - name: jaffle_shop
    description: Raw data from Jaffle Shop
    database: raw
    schema: jaffle_shop

    tables:
      - name: customers
        description: Raw customer data

      - name: orders
        description: Raw order data
        config:
          loaded_at_field: _etl_loaded_at
          freshness:
            warn_after:
              count: 7
              period: day
            error_after:
              count: 14
              period: day